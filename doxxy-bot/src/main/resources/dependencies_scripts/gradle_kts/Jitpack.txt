repositories {
    mavenCentral()

    exclusiveContent {
        forRepository {
            maven("https://jitpack.io")
        }

        filter {
            includeModule("{{group_id}}", "{{artifact_id}}")
        }
    }
}

dependencies {
    implementation("{{group_id}}:{{artifact_id}}:{{version}}")
}
